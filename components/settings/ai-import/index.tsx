'use client';

/**
 * AI Character Import Tab
 *
 * Entry point for the AI character import feature.
 * Used as a CollapsibleCard content on the Aurora page.
 */

import AIImportWizard from './AIImportWizard';

export default function AIImportTab() {
  return <AIImportWizard />;
}
