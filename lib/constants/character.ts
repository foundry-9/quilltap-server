/**
 * Character Constants
 *
 * Client-safe constants for character-related functionality.
 * This file should not import any server-only modules.
 */

// Virtual connection profile ID for user-controlled characters
// When this is selected as the "connection profile", the character is controlled by the user
export const USER_CONTROLLED_PROFILE_ID = '__user_controlled__';
